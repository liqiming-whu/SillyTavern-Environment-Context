# Changelog

## 2.0.2 - 2026-09-07

- 修复原版 SillyTavern 中扩展先于角色数据初始化时，角色卡多选列表为空且不会更新的问题；在角色页、设置加载、应用就绪及短延时窗口自动刷新选项。
- 角色解析兼容数组下标、avatar、ID/UUID、直接角色对象及活动角色字段；直接读取 SillyTavern `script.js` 实时导出的 `active_character`、`this_chid`、`characters` 和 `name2`，不再仅依赖 `getContext()` 中可能为空的 `characterId`。
- 排除 `SillyTavern System` / `TauriTavern System` 系统占位身份，并保留群聊仅允许空绑定配置注入的原行为。
- 刷新角色选项时保留已保存但暂未出现在角色列表中的绑定值，避免初始化竞态导致配置丢失。
- 未匹配状态显示当前角色和已绑定角色，并提示打开绑定角色卡的聊天界面后重试，便于区分真实绑定不一致与解析失败。
- 增加角色列表和多形态当前角色解析回归测试，共 41 项。

## 2.0.1 - 2026-09-07

- 修复点选角色卡后错误提示“当前角色卡未绑定”：绑定值改用稳定的角色 avatar，并兼容 v2.0.0 已保存的数组下标。
- 注入内容拆分为“现实环境信息”“生日和纪念日”“角色生理状态”三个分块。
- 生日和纪念日配置后始终显示日期、下次发生日期和剩余天数；当天显示生日/纪念日提示。
- 经期改为分别配置 `{{user}}` 和 `{{char}}`，注入最近一次经期、下次预计月经来潮和结束日期。
- 修复孕期状态未进入预览的问题；孕期与经期统一归入角色生理状态分块，孕期对象不重复显示经期。
- 增加稳定角色键、非当天纪念日、双对象经期、预计结束日、旧设置迁移和分块提示词回归测试，共 39 项。

## 2.0.0 - 2026-09-07

- 天气提供方新增自动模式，按 Open-Meteo → MET Norway → wttr.in 顺序容错。
- 设置页顶部新增角色卡多选绑定；留空时对全部角色卡注入。
- 新增宏占位符注入，默认宏为 `{{environment_context}}`，并保留三种 `setExtensionPrompt()` 注入方式。
- 新增日历上下文：中国使用内置 `chinese-days` 1.5.9 读取法定节假日、农历和调休；其他地区使用 Nager.Date 公共 API。
- 新增 `{{user}}`、`{{char}}` 生日和自定义纪念日。
- 新增经期设置，可由任意已知起始日与周期长度推算最近一次经期和当前阶段。
- 新增孕期设置，可由怀孕起始日期计算孕周、阶段、状态与预产期。
- 更新 README 的功能、宏用法、隐私说明和匿名化注入示范。
- 新增日历、生理状态、角色卡绑定、宏模式和设置 UI 测试，总计 36 项自动测试。