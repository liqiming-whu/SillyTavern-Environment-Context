{
  "name": "sillytavern-environment-context",
  "version": "2.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check context.js && node --check device.js && node --check weather.js && node --check calendar.js && node --check wellbeing.js",
    "test": "node --test tests/*.test.mjs"
  }
}
