{
  "name": "sillytavern-environment-context",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check context.js && node --check device.js && node --check weather.js",
    "test": "node --test tests/*.test.mjs"
  }
}
